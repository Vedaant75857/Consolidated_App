import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Data Processing Suite — Process. Normalize. Analyze. Deliver.',
  description:
    'Your end-to-end procurement data pipeline. Consolidate, normalize, and analyze procurement data with AI-powered transformations.',
  keywords: ['procurement', 'data pipeline', 'spend analytics', 'data normalization'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  )
}
